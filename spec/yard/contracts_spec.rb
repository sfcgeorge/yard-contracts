require 'spec_helper'

describe Yard::Contracts do
  it 'has a version number' do
    expect(Yard::Contracts::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
